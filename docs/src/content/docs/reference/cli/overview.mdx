---
title: CLI Reference
description: Complete reference for the SOMA command-line interface.
---
import { Aside } from '@astrojs/starlight/components';

## Quick Reference

| Command | Description | Reference |
|---------|-------------|-----------|
| `balance` | Check SOMA balance | [Common Commands](/reference/cli/common/#balance) |
| `send` | Send SOMA to a recipient | [Common Commands](/reference/cli/common/#send) |
| `transfer` | Transfer an object | [Common Commands](/reference/cli/common/#transfer) |
| `pay` | Pay SOMA to multiple recipients | [Common Commands](/reference/cli/common/#pay) |
| `stake` | Stake SOMA with a validator or model | [Common Commands](/reference/cli/common/#stake) |
| `unstake` | Withdraw staked SOMA | [Common Commands](/reference/cli/common/#unstake) |
| `faucet` | Request test SOMA | [Common Commands](/reference/cli/common/#faucet) |
| `status` | Show network status | [Common Commands](/reference/cli/common/#status) |
| `objects` | Query owned objects | [Query Commands](/reference/cli/queries/#objects) |
| `tx` | Inspect and execute transactions | [Query Commands](/reference/cli/queries/#tx) |
| `wallet` | Manage addresses and keys | [Management Commands](/reference/cli/management/#wallet) |
| `env` | Manage environments | [Management Commands](/reference/cli/management/#env) |
| `model` | Register and manage models | [Submission Commands](/reference/cli/submissions/#model) |
| `target` | List targets and submit data | [Submission Commands](/reference/cli/submissions/#target) |
| `validator` | Validator operations | [Operators & Nodes](/reference/cli/operators/#validator) |
| `start` | Start nodes and services | [Operators & Nodes](/reference/cli/operators/#start) |
| `network` | Inspect network configuration | [Operators & Nodes](/reference/cli/operators/#network) |
| `genesis` | Bootstrap a new network | [Operators & Nodes](/reference/cli/operators/#genesis) |
| `keytool` | Low-level key management | [Operators & Nodes](/reference/cli/operators/#keytool) |
| `completions` | Generate shell completions | [Operators & Nodes](/reference/cli/operators/#completions) |

## Transaction Processing Arguments

<Aside>
These flags apply to all state-modifying commands (`send`, `transfer`, `pay`, `stake`, `unstake`, `model commit`, `model reveal`, etc.).
</Aside>

| Option | Description |
|--------|-------------|
| `--tx-digest` | Compute the transaction digest and print it without executing |
| `--simulate` | Perform a dry run (simulation) without executing |
| `--serialize-unsigned-transaction` | Serialize unsigned transaction data as base64 instead of executing |
| `--serialize-signed-transaction` | Serialize signed transaction data as base64 instead of executing |

## Configuration

| File | Description |
|------|-------------|
| `~/.soma/soma.yaml` | Client config: RPC URL, active environment, active address |
| `soma.keystore` | Encrypted keystore for account keys |
| `network-config.yaml` | Local network validator configs (created by `genesis`) |
| `genesis.blob` | Serialized genesis state (created by `genesis`) |
| `fullnode-config.yaml` | Fullnode configuration (created by `start localnet`) |

## Environment Variables

| Variable | Description |
|----------|-------------|
| `RUST_LOG` | Set log verbosity (e.g. `info`, `debug`, `soma=trace`) |
