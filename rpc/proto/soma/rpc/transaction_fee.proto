syntax = "proto3";

package soma.rpc;

import "soma/rpc/object_reference.proto";

// Summary of the fee for this transaction.
message TransactionFee {
  // Total fee deducted (in shannons).
  // Tx fee = unit_fee * executor.fee_units(...).
  optional uint64 total_fee = 1;
}
