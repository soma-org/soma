# Copy to .env (same directory) and fill in. Loaded automatically by
# up.sh, chat.sh, and down.sh. Never commit the real .env.
VAST_API_KEY=
HF_TOKEN=
# MODEL=Qwen/Qwen2.5-1.5B-Instruct
# MAX_DPH=0.4
# MAX_TOKENS=1024
# SOMA_ADDRESS=     # if unset, uses the keystore's first address
